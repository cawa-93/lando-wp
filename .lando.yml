name: lando-wp-experiments
recipe: wordpress

config:
    webroot: web
    php: 8.1

env_file:
    - .env


services:
    appserver:
        run:
            - composer install

    node:
        type: node:18
        command: yarn run dev
        port: 3000
        run:
            - yarn install
